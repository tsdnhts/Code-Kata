class Solution {
    fun solution(n: Int): Int {
        var answer: Int = 0
        
        for (number:Int in 1..n) {
            if(number % 2 == 0) {
                answer += number
            }
        }
        return answer
    }
}
